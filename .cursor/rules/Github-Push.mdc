---
description: 
alwaysApply: true
---

My GitHub repo is git@github.com:mondyv/Wheres-The-Brainrot.git and SSH is configured on this machine. Before starting any new task, always run git pull origin master to get the latest changes. After finishing any changes, always run git add . && git -c user.name="limeladzs" -c user.email="limeladzs@gmail.com" commit -m "description of what changed" && git push origin master. Never skip the pull or push step. If there are merge conflicts, resolve them by keeping the most recent changes.
